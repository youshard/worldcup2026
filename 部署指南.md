# 世界杯预测系统 · 完整部署指南（¥0 方案）

---

## 整体流程

```
本地代码 ──push──▶ GitHub ──触发──▶ Vercel（自动部署网页）
                    │
                    └── GitHub Actions（每30分钟爬数据 ─▶ 自动 push 回 GitHub ─▶ 触发 Vercel 重新部署）
```

**没有任何服务器，完全免费。**

---

## 第一步：注册 GitHub 账号

1. 打开 https://github.com/signup
2. 输入邮箱 → 密码 → 用户名 → 验证 → 完成

---

## 第二步：安装 Git（如果还没装过）

### Windows

下载安装：https://git-scm.com/download/win

安装过程中全部默认 (Next → Next → Finish)

### 验证安装

打开 PowerShell：

```powershell
git --version
# 显示: git version 2.xx.x
```

设置用户名和邮箱：

```powershell
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

---

## 第三步：把项目推送到 GitHub

### 3.1 在 GitHub 创建仓库

1. 登录 GitHub → 右上角 **"+"** → **New repository**
2. Repository name: `worldcup2026`
3. 选 **Public**（免费）
4. 点击 **Create repository**

### 3.2 推送代码

打开 PowerShell（**确保当前在 `worldcup-app` 目录**）：

```powershell
cd e:\新建文件夹\worldcup-app

# 初始化 Git 仓库
git init

# 添加所有文件
git add .

# 提交
git commit -m "世界杯预测系统 v2.0"

# 改分支名为 main
git branch -M main

# 关联 GitHub 仓库（把 YOUR_USERNAME 换成你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/worldcup2026.git

# 推送
git push -u origin main
```

推送时会弹出 GitHub 登录窗口，点授权即可。

---

## 第四步：启用 GitHub Actions

### 4.1 开启 Actions 写入权限

1. 打开 GitHub 仓库页面
2. 点 **Settings** → 左侧 **Actions** → **General**
3. 往下翻到 **Workflow permissions**
4. 选 **Read and write permissions**
5. 点 **Save**

> 这一步让爬虫可以把生成的数据文件自动 Push 回仓库

### 4.2 手动触发一次爬虫

1. 仓库页面点 **Actions** 标签
2. 左侧选 **"数据自动更新"**
3. 点 **Run workflow** → **Run workflow**

第一次运行会：

- 安装 Python + requests
- 执行 `scripts/crawl.py`（拉取天气、排名、模拟比分）
- 把结果写入 `public/data/*.json`
- 自动 commit + push 到仓库

等运行完成后，仓库里 `public/data/` 下会出现 `weather.json`、`rankings.json`、`live-scores.json`。

> 之后每 30 分钟自动运行一次，无需手动操作

---

## 第五步：部署到 Vercel

### 5.1 注册 Vercel

1. 打开 https://vercel.com/signup
2. 选择 **Continue with GitHub**（用 GitHub 账号登录）
3. 授权 Vercel 访问你的 GitHub

### 5.2 导入项目

1. 登录后点 **Add New...** → **Project**
2. 搜索 `worldcup2026` → 点 **Import**
3. 配置页面**不需要修改任何选项**
4. 直接点 **Deploy**

等 1-2 分钟，部署完成会显示：

```
✅ Production: https://worldcup2026-xxx.vercel.app
```

---

## 第六步：验证部署

### 6.1 打开网页

浏览器访问 Vercel 给的链接（如 `https://worldcup2026-xxx.vercel.app`），应该能看到世界杯预测首页。

### 6.2 测试 API

在浏览器打开：

```
https://你的域名.vercel.app/api/bundle
```

应该返回 JSON 格式的实时数据。

### 6.3 测试各个页面

- 首页：`/`
- 球队列表：`/teams`
- 球员：`/players`
- 赛程（点对阵卡片看预测）：`/matches`
- 天气（选日期后自动拉 Open-Meteo）：`/weather`
- 预测：`/predict`

---

## 第七步：分享给朋友

把 `https://你的域名.vercel.app` 发给朋友即可。

---

## 架构说明

### 数据流转

```
scripts/crawl.py                    Vercel Serverless
  （Python 爬虫）                    （Node.js）
      │                                  │
      ├── Open-Meteo 拉天气               ├── /api/live-scores
      ├── FIFA API 拉排名                 ├── /api/rankings
      └── 模拟比赛比分                     ├── /api/weather
            │                             └── /api/bundle
            ▼                                  │
    public/data/*.json                          │
    （JSON 数据文件）                            │
            │                                  │
            └──── Git Push ──▶ GitHub ─────────▶│
                              │                  │
                              └── 触发 Vercel 重新部署
```

### 时间线

| 时间 | 事件 |
|---|---|
| 每 30 分钟 | GitHub Actions 运行爬虫 |
| 爬虫执行后 | 数据 push 到 GitHub |
| GitHub 有变更 | Vercel 自动重新部署 |
| 用户打开页面 | 前端 fetch `/api/xxx` 拿最新数据 |

延迟大约 **2-4 分钟** 从数据更新到用户可见。

---

## 更新代码

以后改了代码，只需：

```powershell
cd e:\新建文件夹\worldcup-app

# 构建
npx vite build

# 提交并推送
git add .
git commit -m "更新xxx"
git push
```

Vercel 会自动检测到 GitHub 推送并重新部署。

---

## 如何在 Vercel 上看日志

1. 打开 Vercel 控制台 → 点你的项目
2. 点 **Deployments** → 点最新的一条
3. 可以看到构建日志（绿色 ✅ = 成功）

---

## 费用总结

| 服务 | 免费额度 | 实际用量 | 费用 |
|---|---|---|---|
| GitHub | 无限公开仓库 | 1个仓库 | ¥0 |
| GitHub Actions | 2000分钟/月 | ~1500分钟 | ¥0 |
| Vercel 部署 | 100GB/月 带宽 | ~1GB | ¥0 |
| Vercel Functions | 100GB-Hrs/月 | ~5GB-Hrs | ¥0 |
| Open-Meteo 天气 | 完全免费 | 每30分钟1次 | ¥0 |
| **总计** | | | **¥0** |
